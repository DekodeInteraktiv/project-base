/**
 * External dependencies
 */
import clamp from 'css-clamp';

/**
 * Spacing styles
 */
export default {
	blockGap: 'var:preset|spacing|md',
	padding: {
		right: clamp(16, 48),
		left: clamp(16, 48),
	},
};
